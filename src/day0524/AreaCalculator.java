package day0524;

public class AreaCalculator {

	/* 1. 클래스 AreaCalculator.java 생성
	 * 	필드 : width, height
	 * 	메서드 : 리턴 타입이 정수이고 매개변수가 2개인 areaResult 생성
	 * 
	 * 2. AreaMain.java 생성
	 * 	Scanner 사용
	 * 	가로 : xxx
	 * 	세로 : xxx
	 * 
	 * 3. 출력 결과 :
	 * 	가로 길이가 xxx이고 세로 길이기 xxx인 직사각형의 면적은 xxx입니다. */
	
	int width;
	int height;
	
	int areaResult(int x, int y) {
		return x * y;
	}
	
}
